[pytest]
markers =
    integration: tests that exercise local OS integrations or create external artifacts
    live_provider: tests that call live AI/image/provider services and require credentials/network
